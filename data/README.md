# Data folder

The CSV dataset is intentionally excluded from git (see `.gitignore`).

To run this project:
1. Download the dataset (or use your own, matching the same column schema).
2. Place the CSV here as `customer_support_tickets.csv`.
3. Run `backend/train_model.py` once to train the prediction model.
